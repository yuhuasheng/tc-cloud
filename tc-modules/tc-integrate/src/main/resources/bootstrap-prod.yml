spring:
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: http://10.203.163.184:8848
        namespace: prod
      # 配置中心配置
      config:
        enabled: true
        server-addr: http://10.203.163.184:8848
        namespace: prod
        file-extension: yaml
        shared-configs: mdas-type-mapping.yaml,tc-integrate-druid.yaml,tc-common.yaml,tc-redis.yaml